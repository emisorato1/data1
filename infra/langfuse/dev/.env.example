# SECRETOS REQUERIDOS (Langfuse v3)
# Documentación: https://langfuse.com/self-hosting/configuration

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""
# Generate with: openssl rand -base64 32
SALT=""
# IMPORTANTE: Debe ser exactamente 64 caracteres hexadecimales. Generate with: openssl rand -hex 32
ENCRYPTION_KEY=""

# SECRETOS DE LA BASE DE DATOS (PostgreSQL, Clickhouse, Redis, S3)
POSTGRESQL_PASSWORD=""
CLICKHOUSE_PASSWORD=""
REDIS_PASSWORD=""
S3_USER=""
S3_PASSWORD=""